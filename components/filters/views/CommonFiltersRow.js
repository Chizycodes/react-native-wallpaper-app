import { Text, View } from "react-native";
import React from "react";

const CommonFiltersRow = ({}) => {
	return (
		<View>
			<Text>Order View</Text>
		</View>
	);
};

export default CommonFiltersRow;
